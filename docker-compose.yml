version: '3.8'

services:
  backend:
    image: zishann555/expense-backend:latest
    container_name: backend
    restart: unless-stopped

  frontend:
    image: zishann555/expense-frontend:latest
    container_name: frontend
    ports:
      - "80:3000"
    depends_on:
      - backend
    restart: unless-stopped